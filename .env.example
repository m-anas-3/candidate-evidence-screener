NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
OPENAI_API_KEY=
# Model for the deep analysis agent (needs reasoning + tool use). Defaults to gpt-5.4-mini.
OPENAI_MODEL=
# Model for the recruiter chat Q&A (simple grounded answers, no tools needed).
# Use a fast, cheap model here — gpt-4.1-nano or gpt-4o-mini work well.
# Defaults to gpt-4.1-nano if left blank.
OPENAI_CHAT_MODEL=
